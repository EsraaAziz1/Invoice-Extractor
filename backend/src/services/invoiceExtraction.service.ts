import { InvoiceData } from '../interfaces/invoice.interface';

export const extractInvoiceFromText = async (): Promise<InvoiceData> => {
  throw new Error('extractInvoiceFromText is deprecated and should not be used.');
};
